---
description: 修复完成后必须自行验证，不能只说"已修好"而未实际确认
alwaysApply: true
---

# Verify After Fix

任何缺陷修复或配置改动完成后，**必须自行执行验证并确认结果**，不能仅告知用户"已修好，请自行验证"。

## 核心要求

1. **自己跑，自己看结果**：修复后必须执行至少一种验证手段（见下表），亲眼确认通过，再告知用户。
2. **验证失败则继续修**：若验证发现问题，立即修复并重新验证，循环直到通过。不要让用户替你验证。
3. **验证结果要展示**：向用户报告时，附上验证的命令/操作和实际输出（如 HTTP 200、测试全绿、页面截图等），而非只说"应该没问题了"。
4. **验证要用用户视角**：如果 bug 是用户在浏览器/UI 中看到的，验证也必须用浏览器/UI 操作，不能只验证 API 就算通过。

## 验证手段（按改动类型选择）

| 改动类型 | 必须执行的验证 |
|----------|---------------|
| 后端代码（Java、Feature、配置） | `mvn test -q`（受影响 BC） |
| 前端代码 | 运行相关 E2E 用例，或用浏览器访问受影响页面确认 |
| Nginx / 代理配置 | `curl` 验证请求链路返回预期状态码 |
| Docker Compose / 部署配置 | 部署后轮询等待服务就绪，再 `curl` 验证核心 API |
| MCP / 智能交互 | 调用 MCP 或 `/api/ai/chat` 验证工具可用、对话正常 |
| 环境变量 / Base URL | 调用实际 API 确认不再出现 Connection refused / 404 |

## 部署场景的特殊要求

若修复涉及已部署环境：

1. 提交推送后，执行 `deploy.sh pull`
2. **主动轮询等待服务就绪**（如每 15s `curl` 一次关键 API，直到 200），不要让用户等
3. 服务就绪后，执行端到端验证（如模拟下单、访问支付页等）
4. 全部通过后，再向用户汇报结果

## 例外

- 纯文档、纯注释改动可不触发
- 用户明确说"不用验证"或"我自己来验证"时可跳过
