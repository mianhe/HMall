---
description: 事件驱动架构约定 — 跨 BC 事件必须通过 Kafka，禁止 Spring 进程内事件
globs: services/**/*.java
alwaysApply: false
---

# 事件驱动架构

所有跨 BC 事件统一通过 Kafka 发布/消费。**禁止**使用 Spring `ApplicationEventPublisher` / `@EventListener` 进行跨 BC 通信。

## 发布事件

1. 在 `application/port/` 定义 `XxxEventPublisher` 接口
2. 在 `infrastructure/kafka/` 实现，用 `KafkaTemplate` 发送
3. 用 `XxxMessage` record 作为 Kafka 消息体
4. topic 名称通过 `@ConfigurationProperties` 配置，命名规则：`<bc>.<event-type>`

## 消费事件

1. 在 `infrastructure/kafka/` 创建 `KafkaXxxEventConsumer`
2. 用 `@KafkaListener` 注解，消息类型为 `Map<String, Object>`
3. 由 `@AutoConfiguration(after = KafkaAutoConfiguration.class)` + `@ConditionalOnBean(KafkaTemplate.class)` 条件注册

## Kafka 配置

- **生产 `application.yml`**：不排除 `KafkaAutoConfiguration`（Kafka 始终启用）
- **测试 `application.yml`**：排除 `KafkaAutoConfiguration`

## 测试

- 出站：`EventCapture` 实现发布端口，`@Primary` 覆盖生产实现
- 入站模拟：直接调用 `XxxEventService.onXxx()`，不用 `ApplicationEventPublisher`

## 参考文档

- 事件契约：`docs/bounded-contexts/<context>/event-flow.md`
- 上下游关系：`docs/context-map.md`
- 集成技术选型：`docs/architecture/integration.md`
- 设计原则 §2.5：`docs/design-principles.md`
