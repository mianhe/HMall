# language: zh-CN
@payment
功能: 创建支付单
  Order 调用创建支付接口，获得 paymentId 与 payUrl；同一 orderId 幂等。

  场景: 使用合法参数创建支付单应成功
    假如 尚无订单 "100" 的支付单
    当 以订单 "100" 金额 "9999" 分 调用创建支付接口
    那么 应返回 201
    并且 响应包含 paymentId
    并且 响应包含 payUrl
    并且 响应中 status 为 "PENDING"

  场景: 同一 orderId 重复创建应幂等
    假如 已存在订单 "200" 的支付单 金额 "100" 分
    当 以订单 "200" 金额 "100" 分 再次调用创建支付接口
    那么 应返回成功（200 或 201）
    并且 返回的 paymentId 与首次创建时相同

  场景: amountCents 为 0 时应返回 400
    当 以订单 "300" 金额 "0" 分 调用创建支付接口
    那么 应返回 400

  场景: 缺少 orderId 时应返回 400
    当 以缺少 orderId 的请求 调用创建支付接口
    那么 应返回 400
