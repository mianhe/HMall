package com.hmall.user.infrastructure.persistence;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SegmentRuleJpaRepository extends JpaRepository<SegmentRuleEntity, Long> {
}
