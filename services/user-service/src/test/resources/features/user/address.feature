# language: zh-CN
@address
功能: 收货地址管理
  作为已登录用户，我希望能够管理我的收货地址，以便结账时快速选择。

  场景: 用户新增收货地址时应成功并返回 addressId
    假如 已存在用户 "alice" 密码 "secret123"
    当 用户 "alice" 新增收货地址 "张三" "13800138000" "广东省" "深圳市" "南山区" "科技园路1号"
    那么 应创建成功
    并且 返回的 addressId 不为空
    并且 返回的地址包含 recipientName "张三"

  场景: 收货地址必填项缺省时应失败并返回错误
    假如 已存在用户 "alice" 密码 "secret123"
    当 用户 "alice" 新增收货地址 "" "13800138000" "广东省" "深圳市" "南山区" "科技园路1号"
    那么 应创建失败
    并且 应返回 400

  场景: 按 userId 查询时应返回该用户的地址列表
    假如 已存在用户 "alice" 密码 "secret123"
    当 用户 "alice" 新增收货地址 "张三" "13800138000" "广东省" "深圳市" "南山区" "科技园路1号"
    当 用户请求用户 "alice" 的地址列表
    那么 应返回 200
    并且 返回的地址列表至少有 1 个
    并且 返回的地址列表中包含 recipientName "张三"

  场景: 按 addressId 查询时应返回地址详情（若属该用户）
    假如 已存在用户 "alice" 密码 "secret123"
    当 用户 "alice" 新增收货地址 "李四" "13900139000" "北京市" "朝阳区" "望京" "阜通东大街1号"
    当 用户 "alice" 请求刚创建的地址详情
    那么 应返回 200
    并且 返回的地址包含 recipientName "李四"
    并且 返回的地址包含 detail "阜通东大街1号"

  场景: 用户修改自己的地址时应成功
    假如 已存在用户 "alice" 密码 "secret123"
    当 用户 "alice" 新增收货地址 "王五" "13700137000" "上海市" "浦东新区" "陆家嘴" "世纪大道100号"
    当 用户 "alice" 将刚创建的地址修改为 recipientName "王五" phone "13700137001" province "上海市" city "浦东新区" district "陆家嘴" detail "世纪大道101号"
    那么 应修改成功
    并且 返回的地址包含 detail "世纪大道101号"
    并且 返回的地址包含 phone "13700137001"

  场景: 用户删除自己的地址时应成功
    假如 已存在用户 "alice" 密码 "secret123"
    当 用户 "alice" 新增收货地址 "赵六" "13600136000" "浙江省" "杭州市" "西湖区" "文三路1号"
    当 用户 "alice" 删除刚创建的地址
    那么 应删除成功
    当 用户请求用户 "alice" 的地址列表
    那么 返回的地址列表中不应包含 recipientName "赵六"

  场景: 地址不存在或不属于该用户时应返回 404
    假如 已存在用户 "alice" 密码 "secret123"
    当 用户 "alice" 请求地址 ID 999999 的详情
    那么 应返回 404
