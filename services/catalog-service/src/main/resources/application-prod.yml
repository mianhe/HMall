spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/hmall
    password: ${DB_PASSWORD:hmall_prod}
  jpa:
    hibernate:
      ddl-auto: update

minio:
  endpoint: http://minio:9000
  secret-key: ${MINIO_PASSWORD:hmall_prod}
