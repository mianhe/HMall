# 商品限定上下文 - 实现步骤

> 先需求与建模，再验收标准（红），再实现（绿）；大步骤分阶段，小步骤可逐条执行。

---

## 脉络总览

```
阶段零：需求与领域建模（已完成）  →  阶段一：验收标准先行（先红）  →  阶段二：四层实现（变绿）  →  阶段三：收尾（可选）
     需求列表 + 领域模型                   feature + steps + 契约            领域 → 基础设施 → 应用 → API      文档与回归
```

---

## 阶段零：需求与领域建模（已完成）

目标：把「要做什么」「领域长什么样」定下来；两者相互对照着做。

| 步骤 | 内容 | 状态 |
|------|------|------|
| 0.1 | **确认和分析需求**：需求列表（管理类别、管理商品及各自场景），见 `catalog/catalog-requirements.md` | ✅ 已完成 |
| 0.2 | **领域建模**：类别与 SPU 聚合、属性、关系、不变式，见 `catalog/catalog-domain-model.md`（图可另存） | ✅ 已完成 |

---

## 阶段一：验收标准先行（先红）← 从这里开始

目标：把「要验收什么」定下来，并跑出红。

目标：把「要验收什么」定下来，并跑出红。

| 步骤 | 内容 |
|------|------|
| 1.1 | 写 **category.feature**（管理类别 4 个场景的 Gherkin） |
| 1.2 | 写 **product.feature**（管理商品 4 个场景的 Gherkin） |
| 1.3 | **约定 API 契约**（类别与商品的路径、请求/响应） |
| 1.4 | 写 **Category Step Definitions**（与 1.1 对应） | ✅ 已完成 |
| 1.5 | 写 **Product Step Definitions**（与 1.2 对应） | ✅ 已完成 |
| 1.6 | **跑验收测试**，确认当前为**红** | ✅ 已完成（8 场景均 404，接口未实现） |

---

## 阶段二：四层实现（变绿）

目标：按 DDD 四层从内到外实现，让验收逐个变绿。

| 步骤 | 内容 |
|------|------|
| 2.1 **领域层** | Category、Spu 实体 + 仓储接口（CategoryRepository、SpuRepository） | ✅ 已完成 |
| 2.2 **基础设施层** | category / spu 表 + 仓储实现（含「某类别是否有子类别」） | ✅ 已完成 |
| 2.3 **应用层** | 管理类别、管理商品的应用服务（创建/查询；创建商品时校验叶子） | ✅ 已完成 |
| 2.4 **API 层** | 类别与商品的 Controller + DTO（按阶段一契约） | ✅ 已完成 |
| 2.5 **验收变绿** | 补全或调整 Step Definitions，跑通全部场景 | ✅ 已完成（8 场景全绿） |

---

## 阶段三：收尾（可选）

目标：文档与回归。

| 步骤 | 内容 | 状态 |
|------|------|------|
| 3.1 | 同步领域模型文档（实体、不变式） | ✅ 已完成（见 `catalog/catalog-domain-model.md` §4 实现对照） |
| 3.2 | 全量跑 Cucumber + 现有接口，做简单回归 | ✅ 已完成（`mvn test` 全绿） |

---

## 执行顺序（一句话）

**阶段零**已完成。接下来：先做完 **阶段一**（feature + 契约 + steps → 红），再按 **2.1 → 2.2 → 2.3 → 2.4 → 2.5** 做阶段二（变绿），最后按需做阶段三。
